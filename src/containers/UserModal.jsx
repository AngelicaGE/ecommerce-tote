import React from 'react'

const UserModal = ({modalStyle, setModalStyle,}) => {
  return (
    <div className={`UserModal modal ${modalStyle}`}>
        user
    </div>
  )
}

export default UserModal